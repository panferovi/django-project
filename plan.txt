ученик:
    решать задачи по теме - предоставить список задач
    решать задачи от учителя - по номеру выдать список задач (Имя + номер)
    посмотреть прогресс - статистика по правильно/неправильно решеным/нерешенным задачам по темам

учитель:
    составить набор задач по теме - выдать номер
    посмотреть статистику по наборам - такие-то наборы решали такие ученики
    посмотреть статистику учеников - такой-то ученик решал такие-то наборы

login:
    mail -> {id, isStudent}

users:
    students:
        id -> student

    teachers:
        id -> teacher;

person {
    id,
    name,
    pass,
}

stats {
}

studentStats {
}

teacherStats {
}

student(person) {
    studentStats
}

teacher(person) {
    teacherStats
}

task {
    desc,
    answer
}

taskSet {
    topic -> task[]
    owner -> db | teacher
}